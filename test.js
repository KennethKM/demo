const numbers = [1, 2, 3, 4, 5];
/*splice = numbers.splice(1, 3);
console.log(splice);*/
slice = numbers.slice(1, 3); /* the last index in a slice acts as 
                            the position of the closing bracket*/
console.log(slice);

const name1 = "\\";
const tada = `My name is ${7 + 3} what's yours?`;
console.log(tada);
for(i=0; i<7;i++){
    const random = Math.floor(Math.random() * 7);
    console.log(random);
}
